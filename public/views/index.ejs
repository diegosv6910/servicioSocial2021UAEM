<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Modulo Indice Reprobacion!</title>
</head>

<body>
    <h1>Generacion de Reportes 1er Semestre Informatica</h1>
    <h2>Indice De Reprobacion Materia</h2>
    <p><label>Selecciona un nuevo archivo!</label></p>
    <input type="file" id="subir_archivo" />
    <div id="resultado"></div>
    <script type="text/javascript" src="resources/js/scripts.js"></script>
    <h3>Selecciona un archivo para generar reporte</h3>
    <% var index=0; %>
        <ol>
            <% for(index; index <=arrayData.length ; index++){ %>
                <li>
                    <a href="/reports/<%= arrayData[index] %>"><%= arrayData[index] %></a>
                </li>
            <% } %>
        </ol>
    <h2>Calificacion por semestre de un alumno</h2>
    <p><label>Ingresa la matricula del alumno!</label></p>
    <input type="text" id="myText">
    <button type="button" id="search-button">Go</button>
</body>

</html>

<script type="text/javascript">
    var searchButton = document.getElementById("search-button");
    searchButton.addEventListener("click", function () {
        var x = document.getElementById("myText").value;
        window.location = `/reports/semestreAlumno/${x}`
    });
</script>